# Backend Environment Variables for Railway Deployment
# Set these in Railway dashboard under "Variables"

# Supabase Database Connection (from Supabase Dashboard -> Settings -> Database -> URI)
DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres?sslmode=require

# OpenAI API Key (from platform.openai.com/api-keys)
OPENAI_API_KEY=sk-...

# SendGrid API Key (from app.sendgrid.com/settings/api_keys)
SENDGRID_API_KEY=SG....

# Verified sender email for SendGrid
SENDER_EMAIL=your-verified-email@domain.com

# Frontend URL for CORS (your Vercel deployment URL)
# Can be comma-separated for multiple origins
FRONTEND_URL=https://your-app.vercel.app,http://localhost:3000

# Optional: Email listener credentials (for IMAP email intake)
EMAIL_ADDRESS=your-email@gmail.com
EMAIL_APP_PASSWORD=your-app-password

# Railway sets this automatically
PORT=8000
